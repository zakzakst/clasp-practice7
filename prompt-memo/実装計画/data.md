## 利用Agent
copilotのPLANエージェント

## やりとり
``` md
input-01.mdの内容をやりたいです。
```

``` md
ありがとうございます。作成した計画をplan.mdに記載していただけますでしょうか。
```

``` md
ありがとうございます。
> WebApp として公開する必要があれば、`doGet` ベースの実装に変更する

WebAppとして公開したいので、上記の対応をお願いできますでしょうか。
変更はplan.mdに反映してください。
```

``` md
こちらのファイルについて、コードリーディングしやすいように修正していただけますでしょうか。
```

``` md
このファイルの処理の流れを簡単に説明していただけますでしょうか。
```

## メモ
- copilotのPLANエージェントは作成したプランをvscodeのmemoryに保存する。gitに残すこと考えるのであれば、ここは調整したい
- PLANエージェントだとShift-JISでの出力になっていた。記載先のファイルの文字コードに合わせるような指示を追加したほうがよさそう
- 「そのまま実装できます」とのメッセージがでたので、PLANエージェントのまま実装に移ってしまった。本来であれば実装用のエージェントに切り替えて行ったほうがいいはずなので、「プランが完成しましたエージェントを切り替えて実装を進めます」的な処理になるようにしたい（エージェントオーケストレーションも検討する）
- レビューの負荷が高い。関数に対してTS Docを記載したり、処理の大きな流れを記載するなど。レビュー負荷が低いコードを出力してもらうようにする？もしくはレビュー用に、概要を出力するプロンプト作る？
- もしくは実装を小分けにするのも一案か？（実際にやるのは結構難しそうな気もする。AIはたくさんのコードを一気に出力してくれるので、コミットを意識的に分けたほうがいいか？）